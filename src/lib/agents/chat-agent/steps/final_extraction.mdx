In the final step, the client application receives the JSON response from Ollama and discards all the technical metadata and JSON formatting. 

The application navigates to `response.message.content` (or parses the text stream if streaming is enabled) and extracts only the plain text response generated by the model. This cleanly formatted text is what is finally rendered on the screen for the user to read.